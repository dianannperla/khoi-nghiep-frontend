import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return <h1>Chào mừng đến với Khởi Nghiệp Frontend!</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));